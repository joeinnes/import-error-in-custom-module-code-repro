// Just export the function, don't do anything fancy... although the import will never be attempted, I'll keep this here to rule out anything weird happening with an import failing.

export default function db(input) {
  console.log(input);
  return;
}